# this conf files must be put in etc/netdata/python.d
# 2018, jbaton


neo4jMonLabels:
    name    :   'neo4jmonrels'
    host    :   'localhost'
    boltport    :   '7687'
    user    :   'neo4j'
    pwd :   'password'
    update_every : 30
    priority: 1  # the JOB's order on the dashboard
    retries: 1       # the JOB's number of restoration attempts
